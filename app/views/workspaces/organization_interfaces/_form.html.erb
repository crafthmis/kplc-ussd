<%= form_for(@organization_interface, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @organization_interface.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@organization_interface.errors.count, "error") %> prohibited this organization_interface from being saved:</h4>

      <ul>
      <% @organization_interface.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :interface, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :interface, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :organization, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :organization, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
