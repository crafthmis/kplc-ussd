<%= form_for(@incidence, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @incidence.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@incidence.errors.count, "error") %> prohibited this incidence from being saved:</h4>

      <ul>
      <% @incidence.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :kind, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :kind, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :explanation, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :explanation, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :customer, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :customer, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
